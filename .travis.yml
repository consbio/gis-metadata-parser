language: python
python:
  - "3.5"
  - "3.4"
  - "2.7"

install:
  - "pip install six"
  - "pip install parserutils"
  - "pip install coveralls"

script:
  coverage run --source=gis_metadata setup.py test

after_success:
  coveralls